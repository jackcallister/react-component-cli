var React = require('react');

require('./${componentDowncase}.less');

var ${componentUppercase} = React.createClass({

  displayName: '${componentUppercase}',

  render: function() {
    return null;
  }
});

module.exports = ${componentUppercase};
